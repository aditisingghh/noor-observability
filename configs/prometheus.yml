global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "node-app"
    scheme: https
    static_configs:
      - targets: ["noor-backend-250j.onrender.com"]
    metrics_path: /metrics